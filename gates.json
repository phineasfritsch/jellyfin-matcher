{
  "_": "Expected numbers for `npm run gate`. Floors, not equalities: the gate fails when a number goes down, because that is what a silent deletion looks like. When work legitimately adds tests or pins, raise these in the same commit.",
  "testFiles": 35,
  "testCases": 564,
  "pinnedClaims": 190
}
