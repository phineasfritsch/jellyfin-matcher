## What this changes

<!-- One or two sentences. What is different for a person using the app? -->

## Gate

- [ ] `npm run gate -- --fast` is green
- [ ] Counts in `gates.json` raised in this commit if tests or pins were added

## Pinned claims

- [ ] No pins changed
- [ ] Pins changed — for each, the `why` says what the new form is and why the property
      still holds. None was weakened to something a blank page would pass.

## If this adds a screen or a control

- [ ] It is reachable by keyboard and named for a screen reader
- [ ] Nothing is communicated by colour alone
- [ ] Anything irreversible says what it costs *before* it is pressed
- [ ] It works at 200% text
